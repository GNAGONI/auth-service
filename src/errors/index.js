const errorTypes = require('./errorTypes');
const validationError = require('./validationError');
const dbQueryError = require('./dbQueryError');
const authError = require('./authError');

module.exports = {
  errorTypes,
  validationError,
  dbQueryError,
  authError,
};
